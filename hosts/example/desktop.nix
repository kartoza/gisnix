{ ... }:
{
  # Host-specific desktop extras for the example host. Empty by default —
  # every host `profiles/cosmic-desktop.nix` is used on needs one of these
  # (even if empty), for parity with the fleet's own hosts.
  imports = [ ];
}
