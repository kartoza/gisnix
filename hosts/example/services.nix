{ ... }:
{
  # Host-specific service overrides for the example host. Empty by default —
  # every host `profiles/services.nix` is used on needs one of these (even
  # if empty), for parity with the fleet's own hosts and with
  # tests/test-example.nix.
}
